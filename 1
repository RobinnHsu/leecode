class Solution:
    def twoSum(self, nums, target):
        complement = {}
        for i, num in enumerate(nums):
            num2 = target - num
            if num2 in complement:
                return [complement[num2], i]
            complement[num] = i
